<section class="container" aria-labelledby="tickets-heading" >
  <div class="ticket-header">
    <div>
      <h1 id="tickets-heading">Tickets</h1>
      <p>Track and update the progress of every request.</p>
    </div>
    <button class="button button-primary" id="new-ticket-btn">New Ticket</button>
  </div>

  <div class="ticket-grid" id="ticket-list" role="list">
    <div class="card table-empty">
      <p>No tickets yet. Create your first ticket to get started.</p>
    </div>
  </div>
</section>

<div id="modal-root"></div>
